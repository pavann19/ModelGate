apiVersion: v1
kind: Pod
metadata:
  name: bad-pod-hostpath
  namespace: default
spec:
  containers:
    - name: main
      image: ${SIGNED_IMAGE}
  volumes:
    - name: hostvol
      hostPath:
        path: /etc
